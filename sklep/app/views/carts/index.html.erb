<h1>Koszyki</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @carts.each do |cart| %>
  <tr>
    <td><%= link_to 'Pokaz', cart %></td>
    <td><%= link_to 'Edytuj', edit_cart_path(cart) %></td>
    <td><%= link_to 'Usun', cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nowy Koszyk', new_cart_path %>
