<h1>Lista</h1>

<table>
  <tr>
    <th>Produkt</th>
    <th>Koszyk</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @line_items.each do |line_item| %>
  <tr>
    <td><%= line_item.product_id %></td>
    <td><%= line_item.cart_id %></td>
    <td><%= link_to 'Pokaz', line_item %></td>
    <td><%= link_to 'Edytuj', edit_line_item_path(line_item) %></td>
    <td><%= link_to 'Usun', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nowy', new_line_item_path %>
